# TRADEON - dependencies
#
# Streamlit Community Cloud uses Python 3.11 (pinned via runtime.txt) so all
# packages below - including Prophet - install cleanly there.
#
# For LOCAL development on Python 3.14, Prophet may not yet have wheels.
# In that case `pip install -r requirements.txt` will fail at the prophet line.
# Workaround: comment out the `prophet` line below for local dev only - the
# app's forecast engine auto-falls back to Holt-Winters + ARIMA when Prophet
# is missing, so everything still works.

# Core data + UI
streamlit>=1.36
pandas>=2.2
numpy>=1.26
plotly>=5.22

# Market data (raw OHLCV only - no external predictions)
yfinance>=0.2.55

# Storage
pyarrow>=15.0

# Forecasting
statsmodels>=0.14          # Holt-Winters, ARIMA, STL
scikit-learn>=1.4          # metrics + scaling
hmmlearn>=0.3              # regime detection
prophet>=1.1.5             # best on big-tech names; Python 3.11/3.12 only

# Dev
pytest>=8.0
